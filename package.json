{
  "name": "digital-diary",
  "version": "1.0.0",
  "private": true,
  "description": "A kid-friendly story recording PWA with AI transcription and text-to-speech",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "busboy": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
